<!DOCTYPE html>
<html>
  <head>
    <title>Wwwgw</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <link rel="stylesheet" href="https://unpkg.com/@picocss/pico@latest/css/pico.min.css">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="container">
      <nav>
        <ul>
          <li><strong>wwwgw?</strong></li>
        </ul>
        <ul>
          <li><%= link_to "search", "/search" %></li>
          <% if current_user.nil? %>
            <li><%= link_to "sign in", new_user_session_url, role: "button" %></li>
          <% else %>
            <li><%= link_to current_user.email, "/user/#{current_user.id}" %></li>
            <li><%= link_to "sign out", destroy_user_session_url, role: "button", class: "outline" %></li>
          <% end %>
        </ul>
      </nav>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <%= yield %>
    </div>
  </body>
</html>
